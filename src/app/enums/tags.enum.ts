// meal tag
export enum DietType {
    VEGAN = "VEGAN",
    VEGETERIAN = "VEGETERIAN",
}

// restaurant tag
export enum SpecialRestaurantTag {
    BAMBILIKES = "BAMBILIKES",
}

// restaurant tag (for MVP)
export enum Cousine {
    ITALIAN = "ITALIAN",
    KOREAN = "KOREAN",
    MEXICAN = "MEXICAN",
    CHINESE = "CHINESE",
    INDIAN = "INDIAN",
    BALKAN = "BALKAN",
}

// meal tag
export enum Taste {
    SPICY = "SPICY",
}

// meal tag
export enum Price {
    CHEAP = "CHEAP",
    EXPENSIVE = "EXPENSIVE",
    REGULAR = "REGULAR",
}

// meal tag
export enum SpecialMealTag {
    CHINESE_SOUP = "CHINESE_SOUP",
}
